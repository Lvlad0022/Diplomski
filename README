The goal is to compare different neural network architectures and reinforcement learning paradigms at solving reinforcement learning problems

upon pulling the environment you should make an environment according to environment.yml and pip install -e. to install q_logic to the envirnoment

you can see a simple snake environment and a trained bot in bot_example.py